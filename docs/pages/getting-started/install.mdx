import Callout from 'nextra-theme-docs/callout';

# Install

Get started with Roover is very simple. Inside your React project directory, run the following:

```
yarn add roover
```

Or with npm:

```
npm install roover
```
